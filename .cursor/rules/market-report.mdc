---
description: 增长2026 复盘报表固定算法、版式与更新流程
globs: generate_report.py,market_news.json,event_catalog.json
alwaysApply: true
---

# 复盘报表 · 固定输出

**项目路径**：`~/Desktop/增长2026/`  
**数据源**：**不依赖** `大盘数据.numbers`。报表日=交易日日历（开盘啦量能 ∪ 长K线交易日，须能覆盖近200个交易日）；量能=开盘啦；新高=问财序列；板块/公告/事件=开盘啦+`market_news`+联网日历；趋势强度/追高/资金认可度=日K+缓存。
**生成命令**：`python3 generate_report.py` → **仅**当日 `复盘概览-{MM-DD}.html` + `index.html`（手机入口）

**历史报表锁定（强制）**：每天生成/更新新报表时 **只写当日 HTML + 首页 index**；**禁止**重写、覆盖、顺带重生成任何已有历史日 `复盘概览-*.html`（含曾作样式锚点的 `07-03`）。共享缓存（如 `count_series_30d.json`）可更新 `as_of`，但不得因此改写旧 HTML。仅当用户**明确点名某历史日**要重修时，才可单独重写该日。

**首页日期列表（固定）**：以最新报表所属月为**当月**平铺展开；非当月按「YYYY年M月」折叠（`<details>`，显示篇数，点击展开）；勿改回全量平铺。

**手机访问（局域网）**：`python3 serve_mobile.py`

**推送**：每次更新报表后 **必须** `git add` 相关文件并 `git push` 到外网（`bash push_github.sh` 或等价命令）；**不要等用户再催**。Token 只需 **repo** 权限。

**Pages 设置**：Settings → Pages → Deploy from a branch → **main** / **docs**（不用 GitHub Actions）

**外网（GitHub Pages）**：https://mcxdcyy.github.io/market-report/

## 已锁定 · 禁止回潮（2026-09-10 定稿 · **2026-09-14 / 2026-09-15 / 2026-09-16 增补锁定** · 每次更新强制）

下列结构、命名、排版与文案已定稿；之后每次更新报表 **只改数据**，**禁止**擅自改版式/算法/回潮旧模块，除非用户明确要求。

**2026-09-15 全日调整已全部定稿（含晚间）**：去大盘表依赖、七段结构、历史新高120日图名与「最新 N 家」、**追高活跃度合计单图**+效应三图近200日均值零轴（上红下绿）、近30日强趋势占比加近200日均值刻度线（上红下绿）、模式结论暂空「待定」。**之后每次更新必须按本文件执行，禁止回潮。**

**2026-09-16 全日调整已全部定稿**：追高活跃度合计单图 + 当日家数柱高；效应三图主板/创板**合并为3张**且近**120**日；问财取数不碰本机 Chrome；每日只写当日报表不改历史 HTML。**之后每次更新必须按本文件执行，禁止回潮。**

### 2026-09-16 增补锁定（已定稿 · 强制沿用）

| 锁定项 | 定稿 | 禁止回潮 |
|--------|------|----------|
| **追高活跃度** | 主板+创板追高家数**合计单图**；柱高=当日家数；近120日；图下方「活跃周期/不活跃周期」 | — |
| **效应三图** | 昨追-赚钱效应 / 昨追-今日承接 / 今追-回落指数各**一张**（主板+创板按家数加权合并）；横轴近**120**日；近200日均值零轴 | 分主板/创板共6图；效应近30日 |
| **页内结构** | 共 **4** 图：追高活跃度 → 赚钱效应 → 今日承接 → 回落指数（均通栏） | 7 图；主板/创板分组标题 |
| **历史报表** | 日常更新 **只写当日** HTML + `index`；`sync_pages_site` 只覆盖当日页+index | 顺带重生历史日；docs 先删光再拷 |
| **图表边框** | 带图下标签的柱图（追高四图、板块-资金认可度）**无卡片边框**，仅顶部分隔线，对齐近30日成交金额 `.vol20-wrap` | 灰底圆角描边卡片把标签框在里面 |

### 2026-09-14 增补锁定（已定稿 · 强制沿用）

| 锁定项 | 定稿 | 禁止回潮 |
|--------|------|----------|
| 追高情绪口径 | 昨追两图用**前一交易日**追高池；(今高−昨高)/昨收、(今收−昨高)/昨高；今追回落用**当日**池 (今收−今高)/今高；图名固定「昨追-赚钱效应 / 昨追-今日承接 / 今追-回落指数」 | 同池算昨追今追；旧图名；改公式 |
| 追高池排除 | 不含 ST/北交所；不含上市日历天数≤10；不含**一字涨停**（最低价=理论涨停价） | 把新股/一字板算进池 |
| 追高数量图 | **追高活跃度**单图（主板+创板合计）；柱高=**当日合计原始家数**；柱色相对合计近**200**日均值（上红下绿）；横轴近**120**日；meta「最新 N 家」=**当日合计原始家数** | 主板/创板分两张数量图；近2日均值柱高；统一中性灰 |
| 追高效应三图窗口 | 昨追赚钱 / 昨追承接 / 今追回落 横轴近**120**日；主板+创板**合并**各一张 | 回潮30日；分主板/创板共6图 |
| 追高图表 meta | 效应图只写「最新 ±X%」；活跃度「最新 N 家」=**当日合计原始家数**；**不写**池子家数；柱顶数字始终隐藏；效应与活跃度均**通栏**；**零轴=近200日均值**（上红下绿） | 标题写「池 N 家」；数量分主板创板；回潮绝对零轴；PC 效应两列分板 |
| 近30日成交金额源 | 与量能120日同用开盘啦实际量能（沪深京）`kpl_market_volume.json` | 回用 `大盘数据.numbers` 成交额列画30日柱 |
| §5 研判摘要 | **已删除**；只保留事件列表；更新时**不要求**写 `direction_analysis` | 恢复节奏峰值/2周主线/当日背景 |
| 第8段命名 | **板块-资金认可度**；柱图近**30**日；末日柱**不加粗** | 「资金认可度」旧名；近20日；末日加粗描边 |

### 2026-09-15 增补锁定（已定稿 · 强制沿用 · **含新高问财 / 去大盘表 / 删10日趋势 / 仅120日新高图+最新N家 / 强趋势30日均值线**）

| 锁定项 | 定稿 | 禁止回潮 |
|--------|------|----------|
| 报表结构 | **七段**：1 **大盘环境** → 2 趋势强度 → 3 资金追高情绪 → 4 涨停板块 → 5 公告与政策 → 6 事件方向 → 7 板块-资金认可度 | 恢复「10日趋势」；八段；把量能/新高塞回已删模块 |
| 大盘环境 | `#sec-mkt`；近30日成交金额 + 量能标签/说明 + 量能120日 + **历史新高120日趋势**（共**3**图+标签说明） | 恢复旧「环境评分」八维卡片 `#sec-env`；删掉三图；恢复「近30日新高数量」双图 |
| **新高数量图** | **只保留**「历史新高120日趋势」；统一中性灰；隐藏柱顶数字；meta = 日期区间 + **「最新 N 家」**（当日原始家数，如 `04/21–09/15 · 最新 10 家`）；量能120日 meta **仍只写日期区间**（不加最新值） | 恢复近30日新高图；新高120 meta 只写区间不加「最新 N 家」；给量能120也加最新值；改回旧图名 |
| 导航 | 首页 / **大盘环境** / **趋势强度** / **资金追高情绪** / 涨停板块 / 公告与政策 / 事件方向 / 板块-资金认可度 | 恢复「10日趋势」入口；漏「大盘环境」；追高与趋势强度顺序回潮 |
| **新高家数（问财）** | 源=**问财问句**（非数据中心 cxg）；历史已写入 `new_high_count_results/series.json`（约 100+ 交易日，**已固化、禁止再从大盘表灌入**）；缺 series 直接报错；每次只拉**报表日**并 `save` 进 `days/`+序列；报表日=今天用「今日…」，否则用「YYYY年M月D日…」；过滤固定：非st/非退市/非北交所/上市天数>10 | 改用 cxg；手填/回读大盘表；非当日仍写「今日」；删掉或覆盖历史 series |
| **报表日 / 交易日轴** | `trading_calendar.py`：开盘啦量能 ∪ 长K线交易日为真源（覆盖≥200日）；更新时 `resolve_report_as_of()`=**当天若交易日用当天，否则最近交易日**；趋势强度近200日均值与追高四图近200日均值**同一交易日轴** | 再用大盘表日期列；日历不足200日仍硬算「200日均值」 |
| **追高四图零轴** | **追高活跃度** + **合并**效应三图 **一律**以近**200**日均值为零轴；柱色均值上红 / 均值下绿 | 数量分主板创板两图；效应分板六图；绝对零轴 |
| **近30日强趋势占比** | 浅色水平刻度线 `.ts-cnt-mark`=近200日**全场**均值（样式同 `.fund-mark`：`border-top: 1px solid rgba(0,0,0,0.12)`）；柱色相对该均值**上红下绿**（非较前日增减）；纵轴须容纳均值 | 回潮较前日红绿；去掉刻度线；改成资金认可度式 50% 线 |
| **量能标签/说明** | `.chart-tags` / `.vol20-tags` / `.vol20-note` 用开盘啦实际量能（与柱图同源）；**全报表图表标签**统一用 `.chart-tags`（字号/padding 同 `.pill`，位置在**对应柱图下方**） | 回用大盘表「成交额/增量」；标签放标题旁；自创异色 pill |
| **数据源** | **整份报表不依赖** `大盘数据.numbers`（生成入口不再读该文件；可从桌面删除） | 恢复必须打开/更新大盘表才能出报表 |
| **表头模式结论** | 暂空占位（`待定` / 无一句总结）；逻辑**待另议**后重写 | 擅自恢复旧 `analyze_trading_modes` 文案当定稿 |

其余锁定项（趋势强度、涨停板块、事件等）仍强制：

| 锁定项 | 定稿 | 禁止回潮 |
|--------|------|----------|
| 报表结构 | **七段**（见上） | 独立「环境评分」八维卡片、`#sec-env`、「开仓建议」、§「题材方向」优先/可做；恢复「10日趋势」 |
| 导航 | 见上 | 导航出现「环境评分」「开仓建议」「10日趋势」；写回「资金认可度」 |
| ~~模块1 八维~~ | **已删除**（原10日趋势） | 恢复八维表 / `#sec-trend` |
| 命名 | 个股五条件占比模块名 = **趋势强度**；新高柱图=**历史新高120日趋势**（问财） | 把历史新高家数叫「趋势强度」；改回旧图名 |
| 强趋势脚注 | 固定一句（见「趋势强度模块」） | 编号列表、样本加权长说明、「须同时满足」旧措辞 |
| 趋势强度图表 | 「近30日强趋势占比」须带近200日均值刻度线+上红下绿；横条行距约 12px、条高略松；刻度默认 **10%**（超则按当日最大）；右侧占比相对近200日均值红/绿；条内竖线标均值；无偏强/偏弱 pill；无图表下方重复数字卡 | 30日图回潮较前日色或无均值线；挤成 8px 行距、恢复 pill、图表下再堆一套卡片、改回按板块最高缩放且无均值竖线 |
| 成交额结构色 | ≥5亿 **紫色** `#9A7EAD`、&lt;5亿 `#E2C49A` | 改回蓝色 `#5B8FB9` 或其他色系 |
| §事件 结构 | **仅事件列表**；**无**研判摘要、**无**时间轴、**无**「事件详情」小标题 | 恢复研判摘要 / 时间轴 / 题材方向 |
| §事件 模块头 | 右侧一行：`{窗口} · 已同步N条` | 「沿用本地」；旧长句 |
| §事件 红蓝点 | 红=重要 / 蓝=一般；`_event_is_hot` = seed.hot **或** importance≥**4** **或** 产业链级IPO | 会议名关键词硬编码；门槛改回 3 |
| §事件 日期/周几 | 日期字号略大；单日/区间相对周几规则见下 | 区间只写起始周几；标题/brief 再塞日期 |
| §事件 brief | 标题=事件名；`brief`=增量事实；会议类须写清议题 | 复读标题；「产业顶会窗口」 |
| §事件 覆盖 | 顶会/政策/业绩窗口/**产业链级IPO** | 临近才补；删掉事件描述 |

## 报表七段（固定）

| # | 模块 | 说明 |
|---|------|------|
| 1 | 大盘环境 | 「近30日成交金额」+ 量能标签/说明 +「量能120日趋势」+「历史新高120日趋势」 |
| 2 | 趋势强度 | 全场+主板/创业板/科创板/北交所/ST：近30日强趋势占比 + 横条 + 成交额结构 |
| 3 | 资金追高情绪 | **追高活跃度** + 合并效应三图（共 4 图，均近120日） |
| 4 | 涨停板块 | 开盘啦清单 + 自写逻辑/后续/预判 |
| 5 | 公告与政策 | 盘后披露 + 当日重要国家政策 |
| 6 | 未来2周 · 事件与方向 | **仅事件列表** |
| 7 | 板块-资金认可度 | 开盘啦近30日板块池占比柱图 |

**表头（hero）**：策略标签保留「5/10日线试单 & 新高加仓」；**模式结论暂为占位「待定」**（逻辑另议）；无一句总结时不渲染 `.hero-summary`。**已删除**「10日趋势」、独立环境评分八维卡片、「开仓建议」。

### 资金追高情绪模块（固定 · `#sec-chase` · 每次更新强制）

由 `chase_sentiment.py` 统计，结果缓存 `chase_sentiment_results/YYYY-MM-DD.json`；生成最新交易日报表时重算。

| 项 | 口径（勿改） |
|---|---|
| 追高定义 | （当日最高价 − 前一日收盘价）/ 前一日收盘价 **≥ 7%** |
| 主板追高 | 沪深主板个股追高池（**不含** ST、北交所；**不含**上市日历天数≤10；**不含**一字涨停） |
| 创板追高 | 创业板 + 科创板追高池（**不含** ST、北交所；**不含**上市日历天数≤10；**不含**一字涨停） |
| 一字涨停排除 | 当日最低价 = 当日理论涨停价（昨收×涨停幅度，四舍五入到分）；昨追池看**前一交易日**是否一字，今追池/数量看**当日**是否一字 |
| 追高活跃度 | **主板+创板**追高家数合计；柱高=**当日合计原始家数**；柱色相对合计近**200**日均值（上红下绿）；标题固定「追高活跃度」；meta「最新 N 家」=**当日合计原始家数**；横轴近**120**日；**图下方**标签（同近30日成交金额 `.chart-tags`）：近**5**日均值≥近200日均值→「活跃周期」，否则「不活跃周期」 |
| 昨追-赚钱效应 | 主板+创板池**按家数加权合并**；取**前一交易日**追高池；当日 (今高 − 昨高) / 昨收；柱色相对近**200**日均值 |
| 昨追-今日承接 | 同上加权合并；取**前一交易日**追高池；当日 (今收 − 昨高) / 昨高；柱色相对近**200**日均值 |
| 今追-回落指数 | 同上加权合并；取**当日**追高池；当日 (今收 − 今高) / 今高；柱色相对近**200**日均值 |
| 窗口 | **追高活跃度**与**效应三图**均为近 **120** 个交易日 |
| 页内结构 | **追高活跃度** → **昨追-赚钱效应** → **昨追-今日承接** → **今追-回落指数**；共 **4** 张柱图（均通栏，不再分主板/创板） |
| 图表 | 四图均以近**200**日均值为零轴（上红下绿）。活跃度柱高为当日合计原始家数；meta「最新 N 家」。效应图 meta **只写最新值**；**不写**池子家数；**始终隐藏**柱顶数字；窄屏单列 |
| 脚注 | 固定算法说明一句（页面 `.chase-note`）；须含活跃度合计口径、效应加权合并、上市≤10日与一字涨停排除 |

**已定稿（每次更新强制）：** 追高活跃度为合计单图、柱高为**当日合计原始家数**；效应三图为主板+创板加权合并、近**120**日；近5日均值标签「活跃周期/不活跃周期」；近200日均值零轴。**禁止**擅自改回分板六图、效应近30日、近2日均值柱高或绝对零轴，除非用户明确要求。

### 模块1 · 10日趋势 · 八维（固定 · `#sec-trend`）

横轴列顺序（`TREND_SCORE_KEYS`，不可改序/改名）：

| 分组 | 列名 | 数据含义 |
|------|------|----------|
| **环境 · 活跃**（前4） | 大盘量能 | 成交金额规则分（非历史分位） |
| | 新高指标 | `新高` 列 · 历史新高家数 · `hybrid_hist_score` |
| | 主板活跃 | 主追高数 |
| | 创板活跃 | 创活跃 |
| **追高效应**（后4） | 主板昨追 | 主赚差 |
| | 创板昨追 | 创赚差 |
| | 主板今追 | 主冲高未回落占比 |
| | 创板今追 | 创冲高未回落占比 |

**分组视觉（固定）：** 表头两行——第一行分组名「环境 · 活跃」「追高效应」；第二行八维列名；两组之间用 `trend-split` 竖分隔线；两组表头底色略区分（前组偏蓝灰、后组偏紫灰）。**两行表头字号/字重/字距/内边距统一**（均为 12px / 700 / `0.02em` / `8px 10px`），仅靠底色区分层级，**禁止**分组行缩小字号或收紧 padding。**禁止**改回无分组的一排八列平铺。

**页内顺序（固定）：** **仅**八维表。量能/新高图已迁至 **大盘环境**（`#sec-mkt`）。
**禁止展示：** `trend_headline` / 「MM/DD→MM/DD：昨追赚钱效应偏弱。当前处于退潮/弱势段。」等 10 日窗口总结 callout（后台 `analyze_10d` 可仍计算，**页面不渲染**）。  
后台仍可算 `env_callout` / `scores` 供模式判定；**页面不渲染**独立环境评分八维卡片与上述 trend_headline callout。

### 大盘环境模块（固定 · `#sec-mkt` · 每次更新强制）

位于「10日趋势」之后、「趋势强度」之前。页内顺序（固定）：

「近30日成交金额」柱图 → `.vol20-tags` 量能标签（有则展示、无则整行不渲染）→ **「量能120日趋势」** → `.vol20-note` 量能说明 → **「历史新高120日趋势」**（模块最底部）。

**量能标签** `.vol20-tags`（有则展示、无则不渲染；**禁止与说明文案重复堆砌**）：
- 差：按优先级只留 **1** 条——`连续缩量N日` → 否则 `连续2日总缩量X%` → 否则 `缩量低于前三日均`（不叠多条同义缩量）
- 好：`放量突破`（连续放量且破前高）→ 否则 `连续放量N日` / `突破前三日高量` 二选一
- 中性：不打标签
- 「资金在场偏弱」：有结构标签时只写在 `.vol20-note` 末尾；无结构标签时才单独成标签
- **说明** `.vol20-note`：只写量能数字句（当日万亿 / 近3日均 / 较前日），**不再**复述标签里的缩量/放量理由；数据源=**开盘啦**实际量能（与柱图同源），**禁止**再用大盘表成交额算说明
- **样式**与 A 股红强绿弱同一套 `.pill`（配色缩量偏弱=绿、放量偏强=红）

**禁止**：把量能/新高图塞回 `#sec-trend`；恢复旧 `#sec-env` 环境评分八维卡片；恢复「近30日新高数量」。

### 趋势强度模块（固定 · `#sec-ts` · 每次更新强制）

由 `trend_strength.py` 统计，结果缓存 `trend_strength_results/YYYY-MM-DD.json`；生成报表时对最新交易日自动计算。

**算法条件（五条同时满足，勿改阈值）：**

1. 连续 3 日收盘价在五日线上方  
2. 连续 5 日最低价在十日线上方  
3. 近 3 日最高价 = 近 20 日最高价  
4. 今日五日线 > 昨日五日线（严格大于）  
5. 排除当日跌停  

**维度（排版顺序）：** 全场 → 主板（沪+深）→ 创业板 → 科创板 → 北交所 → ST。  
**全场：** 各板块按样本家数加权的均值（凸组合），介于板块最低与最高之间，不会单独高于最高板块；「最高」标签只打在五板块上（不打在全场）。  
**页内结构：** 「近30日强趋势占比」柱图 → 「强趋势占比」横条图 → 「强趋势股成交额结构」100% 堆叠（按家数：≥5亿 / 5亿以下，条宽与右侧数字均为家数）→ **脚注一句**。  
**近30日强趋势占比：** 全场强趋势个股占比（与下方横条「全场」占比同口径）；纵轴为每日占比%（非家数）；缓存 `trend_strength_results/count_series_30d.json`；生成最新交易日报表时由 `ensure_count_series_30d` 对齐 `as_of` 重算；**浅色水平刻度线 `.ts-cnt-mark`=近200日全场均值**（样式同板块-资金认可度 `.fund-mark`）；柱色相对该均值（上红下绿，**非**较前日增减）；纵轴须容纳均值（下限=窗口与均值的最小值×0.85、上限=窗口与均值的最大值）；柱顶/标题 meta 显示百分比；悬停可看家数；手机端隐藏柱顶数字、禁止横滑。
**近200日均值：** 生成**最新交易日**报表时自动对齐 `as_of` 重算并写入 `trend_strength_results/means_200d.json`（各维度独立均值；30日柱图刻度线、横条内竖线与右侧红绿均相对该均值）；历史日重生成不覆盖该缓存。  
**脚注（固定原句，勿改）：** 
`强趋势定义（同时满足）：连续3天收盘价在五日线上方，连续5天最低价在十日线上方，3日内创20日新高，五日线向上，非跌停。` 
**视觉（固定）：** `.ts-hbar` / `.ts-amt-row` 行距约 **12px**；占比条高约 22px、堆叠条约 24px；图表内边距略松；**刻度**默认固定满格=**10%**，若当日任一行占比>10%则满格=当日最大值；**右侧占比数字**相对该维度近200日均值着色（低于=绿 `#34C759`、高于=红 `#E53935`）；**条内竖线** `.ts-hbar-mean` 标近200日均值位置（均值缓存 `trend_strength_results/means_200d.json`）；**不要**偏强/偏弱 pill；**不要**图表下方再堆一套重复数字卡；成交额色：≥5亿 **紫色** `#9A7EAD`、&lt;5亿 `#E2C49A`（禁止改回蓝色 `#5B8FB9`）。 
**ST 互斥：** ST 只进 ST 桶，不计入其余板块桶；全场含全部有效样本。 
**样本：** 上市日历天数 > 10；须有至少 20 根日 K 且当日有成交。 

**已定稿（每次更新强制）：** 「近30日强趋势占比」均值刻度线 + 上红下绿、横条相对近200日均值红绿与条内竖线，**禁止**擅自改回较前日着色或去掉均值线，除非用户明确要求。

策略语境：非打板；5/10 日线试单 & 新高加仓；不行就空仓。

**模式语义（评估开仓时强制对齐，勿搞混）：**

| 对象 | 「新高」含义 | 用途 |
|------|-------------|------|
| `大盘数据.numbers` | **已脱离**：日常更新与生成**不读**该文件；可从桌面删除 | 误恢复「必须填大盘表才能出报表」 |
| 用户个人执行：「新高加仓」 | **2 日内新高**（非历史新高 / 非 ATH） | 评估「次日是否适合按该模式开仓/加仓」时，按 5/10 日线强趋势试单 + **2 日内新高加仓**理解 |

策略标签文案保持「5/10日线试单 & 新高加仓」**不必改**。助手在口头研判、持仓复盘、开仓反馈时，**禁止**把用户模式理解成「追历史新高/创新高板」。

### 板块-资金认可度模块（固定 · `#sec-fund` · 每次更新强制）

由 `fund_recognition.py` 统计，结果缓存 `fund_recognition_results/YYYY-MM-DD.json`；生成最新日报表时重算。个股池来源：开盘啦涨停板块 codes，缓存 `kpl_sector_stocks_history.json`（由 `fetch_kpl_sectors.py` 写入）。

| 项 | 口径（勿改） |
|---|---|
| 模块名 | 页面标题与导航固定为 **板块-资金认可度**（禁止回潮「资金认可度」） |
| 板块范围 | 近 **30** 个交易日开盘啦涨停原因中出现过的全部板块（不限 ≥5） |
| 个股池 | 按板块名合并这 30 日内曾上榜该板块的全部代码（去重） |
| 按日指标 | 池内当日成交额 **>3 亿元** 为分母；分子须同属该集合，且「收盘 > MA5」且「收盘 > MA10」；占比 = 分子/分母 |
| 空样本 | 分母为 0 → 该日不画柱 |
| 展示过滤 | 池内个股数 **&lt;10** 不展示；**永久排除**（勿再改回展示）：①名称含 **ST** 的板块 ②**并购重组** ③**中报增长** ④**实控人变更**；**报表当日**（序列末日）成交额&gt;3 亿家数 **严格 &lt;10** → 不展示该板块整张柱图；自报表日起 **连续 10** 个交易日占比均 **&lt;50%**（无样本日视为未达 50%）→ 不展示 |
| 展示排序 | 当日占比 **&gt;50%** 优先，组内按当日占比降序；当日占比 **≤50%** 按近 **5** 个交易日占比均值降序（无样本日不计入均值） |
| 卡片文案 | `.fund-card-meta` 固定为「共 N 只 · 3亿以上 X 只」（N=池内个股数；X=报表当日成交额&gt;3 亿家数） |
| 图表 | **每板块一张**柱图；横轴近 **30** 个交易日；纵轴百分比（满格 100%）；**50%** 处浅色水平刻度线（`.fund-mark`）；柱色 A 股惯例 **&gt;50% 红 / ≤50% 绿**；**图下方**「占比 X%」标签（`.chart-tags`，样式同近30日成交金额量能标签；&gt;50% 红 / ≤50% 绿）；**不展示**较前日升降箭头；**末日柱不加粗、无描边**（与其余柱同宽）；PC≥960px **两列**等宽（`.fund-list`：`repeat(2, minmax(0,1fr))`）；窄屏单列；**禁止**回潮标题旁 fund-pill |
| 排序（池构建） | 近 30 日累计涨停家数降序（后台聚合用；页面展示顺序见「展示排序」） |
| 位置 | 第 8 段，在「未来2周 · 事件与方向」之后；导航「板块-资金认可度」 |
| 顶部提示 | 模块标题下固定一句（`.fund-tip`，勿改）：`【强势票占比很高】：也有可能突然一下崩。——重点看「核心票」。`；**无**左边竖条、左对齐；手机端（≤640px）第二句 `——重点看「核心票」。` 换行单独成行 |
| 脚注 | 固定算法说明一句（页面 `.fund-note`）；文案冠名「板块-资金认可度」；序列口称为近30日 |

**已定稿（每次更新强制）：** 只重算并刷新数据，**禁止**擅自改算法阈值、展示过滤、排序、柱色、卡片文案或版式，除非用户明确要求。

**禁止**：把同花顺自选纳入本模块；改现有涨停板块展示过滤/逻辑文案规则。

### 表头 hero（固定 · 每次更新强制）

结构（上 → 下）：

| 区块 | 内容 | 禁止 |
|------|------|------|
| 标题区 | `复盘报告 · MM/DD` + 数据日/下一交易日 | — |
| 策略标签 | 固定文案「5/10日线试单 & 新高加仓」（蓝色 pill） | 改文案 |
| 模式结论 | **暂空占位**：状态 `—` + `待定`；总结空则不渲染 `.hero-summary` | 擅自恢复旧主用/试单/空仓算法当定稿（另议后再写） |
| 导航 | 首页 / 大盘环境 / 趋势强度 / 资金追高情绪 / 涨停板块 / 公告与政策 / 事件方向 / 板块-资金认可度 | 「10日趋势」「开仓建议」「环境评分」；写回「资金认可度」 |

**总结文案规则（模式逻辑重写后沿用；当前占位阶段可为空）：**

- 写清具体原因，例如空仓且承接不够 →「承接不足，空仓等待昨追赚钱效应修复、新高回升。」
- 承接已够但结构未达标 →「昨追效应与新高未达标，空仓等待修复后再试单。」
- **禁止**「模式或承接不足」等二选一模棱表述
- 总结里**不要**再出现「模式」二字（上方主标签已是模式结论）
- **样式固定**：颜色 `var(--sub)` 灰黑；**不加粗**（`font-weight: 400`）；桌面 **12px**、手机 **14px**
- **禁止**在表头再加：承接/模式得分、判断依据标签、极端预警块、大进度条

**手机端交互（固定）：**

- 表头**禁止吸顶**：`.hero` 须 `position: static !important`（勿恢复 `sticky` / `fixed`）
- 首页报表链接可带 `?v=` 防缓存参数；生成后若改过 hero CSS，须确认 `docs/` 最新页已无 `sticky`

### 文案语气（固定 · 每次更新强制）

报表全部对外可见文案（含 `market_news.json` 写入的 `reason` / `events` / `post_close_pool` / `SEED` brief、表头总结、板块逻辑与后续事件等）须用**官方书面语**，像正规研报/公告表述。

| 要求 | 说明 |
|------|------|
| **要用** | 完整规范词、书面因果句；机构名/会议名/政策名写全称（必要时括号补英文缩写一次） |
| **禁止** | 口头简称、行话缩略、聊天口吻、口号式短切 |

**禁止示例 → 书面替换（不穷尽，同类照此）：**

| 禁止（口头/缩略） | 改用（书面） |
|------------------|--------------|
| CXO / CRO / CDMO | 医药合同研发（生产）组织、临床研究外包、合同定制生产等全称 |
| BD / License-out | 对外授权合作、License-out 对外授权（首次可保留英文全称语境） |
| PCB / CCL / CPO | 印制电路板、覆铜板、共封装光学等全称 |
| 缩圈 | 聚焦龙头、向核心标的集中 |
| 高低切 | 资金从高位题材转向相对低位方向 |
| 一日游 | 缺乏持续性、短线冲高回落 |
| 抽血 | 分流资金、对板块形成资金抽离 |
| 验牌 | 业绩与订单验证、财报验证 |
| 中军 / 高标 / 抱团 | 板块权重股或龙头、阶段高位标的、资金集中持仓 |
| 接力 / 跟风 | 延续上攻、跟随上涨 |
| 炸板（作口语谓语时） | 涨停后打开、封板失败（数据字段名可保留） |
| 盯 / 看能不能 | 关注、观察是否 |

**例外（可保留）：**

- 已锁定的固定 UI 标签与策略文案（如「5/10日线试单 & 新高加仓」「不认可持续」「预估持续2天」）
- 板块正式名称本身（如开盘啦返回的「AI应用」「智能电网」）
- 法规/会议官方英文缩写在全称后首次括注（如「世界人工智能大会（WAIC）」）
- 代码与 JSON 字段名、股票代码

## 用户触发表格/报表更新时（必做全套）

**不需要每日自动跑**；用户要求更新时，助手必须一并完成：

1. **报表日**：`trading_calendar.resolve_report_as_of()`——当天是交易日用当天，否则最近交易日
1b. **新高家数（问财 · 每次强制）**：助手用 **Cursor 内置浏览器**跑问句（**勿开本机 Chrome**），**日期必须对齐报表日**：
   - 报表日 = **今天** → `今日最高价创历史新高，非st，非退市，非北京证券交易所，上市天数大于10天`
   - 报表日 ≠ 今天（例：周日补做周五报表）→ `YYYY年M月D日最高价创历史新高，非st，非退市，非北京证券交易所，上市天数大于10天`（用**报表日**，禁止写「今日」）
   → `python3 new_high_count.py save --date YYYY-MM-DD --count N --names …`  
   写入 `new_high_count_results/days/` 与 `series.json`；**禁止**让用户口述家数
1c. 运行 `python3 generate_report.py`（**无需**打开/更新大盘数据.numbers；**只生成当日**，勿重写历史日 HTML）
2. **涨停板块（固定 · 每次更新强制）**：开盘啦只定「有哪些板块 / 几家 / 哪些票」；**逻辑文案必须全网核实后自写**
   - **板块清单（开盘啦 · 与出图脚本同逻辑）**：`python3 fetch_kpl_sectors.py [YYYY-MM-DD]`（对齐 `~/Projects/stock-analysis/export_sector_images.py`）
     1. **优先** `DailyLimitResumption` / `GetPlateInfo_w38`；`name/count/codes/stocks` 以 `ZSName` + `StockList` 为准（不依赖 App 打开）
     2. **实时兜底（固定）**：若 `GetPlateInfo` 空列表 / 失败 / 返回日期不是目标日（且目标日是今天）→ 改走 `HomeDingPan` / `DailyLimitPerformance`（PidType 1–20），按个股「涨停原因」字段聚合板块；**禁止**因主接口空列表就直接放弃开盘啦改读截图
   - **备用清单**：上述开盘啦主路径+实时兜底**均失败**时，才读 `~/Desktop/同花顺板块/YYYYMMDD/` 或 `~/Desktop/开盘啦板块/YYYYMMDD/`（该目录 JPG 亦由同一套开盘啦逻辑出图）；**禁止**实时读屏当主路径
   - **禁止**：用东财涨停池自行拆分/重命名开盘啦板块名充当展示列表
   - **逻辑 `reason`（固定 · 每次更新强制 · 禁止照抄开盘啦）**：开盘啦只提供板块索引；**必须全网检索**当日催化（研报/新闻/公告/复盘）后自写；**可参考「韭研公社」（原韭菜公社）每日市场逻辑精选**及格隆汇/主流财经复盘，交叉核对后写成自己的表述；**措辞须符合上文「文案语气」书面语规则**
     - **禁止**：开盘啦个股「涨停原因」原文、「开盘啦归并为…」套话、只改措辞的伪原创
     - **要写**：硬催化（政策/研报上修/业绩/出海订单等）+ 龙头验证 + 一句结构判断
   - **后续事件 / 预判（每次更新强制）**：结合日历、盘后公告与已核实逻辑自写；勿用开盘啦标签句凑数；书面语，禁用口头简称
   - **写入** `market_news.json` → `top_sectors`（name/count/reason/codes/stocks[/events]）
   - **展示过滤（固定 · 每次更新强制 · `filter_display_sectors` · 两段流水线）**：
     1. **资格筛选**
        - 若存在涨停家数 **≥5** 的板块 → 只保留这些（按家数降序；可能只有 1–3 个，不强制凑满）
        - 若没有任何 ≥5 → 只保留家数**并列最高**（最强可能不止 1 个，例如两个都是 4 家则都保留）
     2. **数量上限**
        - 对上一步结果再按家数降序，默认**最多展示 5 个**
        - 若第 5 与第 6 家数相同 → **允许一并展示第 6**（及之后与第 5 家数相同的并列）
     - JSON 可多写候选，页面以过滤结果为准；**禁止**拆开两段或只保留其中一段
   - **已持续天数**：按下方「已持续天数」规则，用开盘啦历史涨停原因家数回溯（可先 `python3 fetch_kpl_sectors.py YYYY-MM-DD --history 14`）
   - **板块序号样式（固定 · 每次更新强制 · `.sector-rank`）**：**浅蓝色实心圆**（`#64B5F6` 底 + 白字，`border-radius: 50%`）；与报表模块 `.section-num` **实心方块（深一点的 accent 蓝）**区分；**禁止**改回方块/空心/深色实心方块
3. **公告与政策**：同文件 `post_close_pool`，由 `curate_post_close` 精选
   - **上市公司公告**：报表当日 **15:00 收盘后** 正式披露（`timing`: `7/8盘后` 或 `盘后`）
   - **国家政策**：报表当日公开发布的重要文件（国务院/部委规章、出口管制、产业规划等）；`type`: `政策`，`timing` 用 `7/X发布`、`7/X印发`、`7/X盘后` 等标注当日
   - **禁止**写入：前一交易日盘后、无 `timing` 的旧闻复述、盘面点评
   - 代码层 `_is_same_day_post_close` 会自动过滤不符合项（政策允许当日发布，不要求收盘后）
5. **未来2周 · 事件与方向**：按 §5 **仅更新事件列表**（联网抓取事件库 + seed）；**不再**要求写/展示 `direction_analysis` 研判摘要（`summary`/`peak`/`rhythm`）
   - **事件库必须联网抓取**（见下「事件库同步」）；抓取失败须带网络权限重试，**禁止**沿用 `event_catalog.json` 完成本次报表
   - JSON 里旧的 `direction_analysis` 可保留不删；页面不渲染；`rhythm` 仅作后台补里程碑可选输入
6. **推送外网**：`git add`（含 `docs/`、`market_news.json`、`generate_report.py` 等）→ `commit` → `git push`；完成后给出 https://mcxdcyy.github.io/market-report/ 链接。**禁止只更新本地不 push。**

## 公告与政策（固定标准 · 每次更新必做）

第 6 模块 **永远** 同时收录两类信息，不可只写上市公司预告/公告：

| 类型 | 要求 | `post_close_pool` 写法 |
|------|------|------------------------|
| 上市公司 | 报表当日 **15:00 收盘后** 正式披露 | `type`: 预告/公告，`timing`: `7/X盘后` |
| 国家政策 | 报表当日公开发布、对板块有炒作价值的文件 | `type`: `政策`，`category`: `D`，`timing`: `7/X发布` / `印发` / `盘后` |

**政策示例**：出口管制（如 7/10 氦气）、国务院批复/部委规章、基药目录、产业规划等。

**每次更新报表时**：检索当日收盘后公告 **+** 当日重要政策，一并写入 `post_close_pool`；晚间批次不全可次日补，但不可漏掉已公开的重大政策。

## 未来2周 · 事件与方向（固定标准 · 2026-09-14 定稿 · 每次更新强制）

第 7 模块 **永远** 只渲染 **事件列表**（由 `render_fwd_section_html` 生成）：

| 层级 | 页面区块 | 作用 |
|------|----------|------|
| 1 | **事件列表** `fwd-cal-wrap` | **上下排布**（日期行 → 标题 → brief 通栏）；**无**「事件详情」小标题；日期前红/蓝实心圆；周几见下；brief 见下 |

**已删除（禁止回潮）：** **研判摘要** `fwd-lead-wrap`（节奏峰值 / 2周主线 / 当日背景）；横向时间轴 `fwd-track-wrap`；「事件详情」小标题；「题材方向」`fwd-themes-wrap` / `themes[]` / 优先·可做·观察·回避 stance。

### 模块头（固定）

`.section-sub.event-meta` 单行右对齐（手机左对齐）：

- 窗口：`event-window`（如 `9/14–9/28`）
- 同步：`event-sync-note` 文案固定为 **`已同步N条`**（无空格、不写「日历线索」）；CSS `::before` 出间隔点 `·`
- 出现「沿用本地」→ 本次更新不合格，须重抓后重生

### 红 / 蓝点（固定 · `_event_is_hot` · `EVENT_HOT_MIN_IMPORTANCE = 4`）

日期前 `.fwd-cal-mark`：重要=红 `#ff6b63`（日期字亦偏红）；一般=蓝 `var(--accent)`。

| 标红条件（命中任一） | 说明 |
|------|------|
| seed / 上游 `hot=True` | 人工认定的强节点 |
| 日历 `importance ≥ 4` | 华尔街见闻通用评级（1–4），随新事件自然适用 |
| 产业链级 IPO/申购 | `_event_is_chain_ipo` 结构规则 |

**禁止**用 WAIC / 吹风会 / 中报 / 政治局等**具体会议名关键词**硬编码标红（无通用性）。未达以上 → 蓝点。

### 日期与周几（固定）

- 日期 `.fwd-cal-md` 字号略大于正文（桌面 **14px** / 手机 **16px**）
- 周几 `.fwd-cal-wd`：相对报表日、周一为一周起点
  - 单日：`本周二` / `下周三` / `下下周X`
  - 区间：同周 `本周一 至 周三`；跨周 `本周五 至 下周一`
- **日期只出现在日期列**；标题与 `brief` **禁止**再写 `9月16日` / `9/16`

### 事件库同步（固定 · 每次更新强制）

| 要求 | 说明 |
|------|------|
| **禁止沿用本地事件库** | 不得用 `event_catalog.json` 旧缓存充当本次列表来源；该文件仅可作本次同步**写出**结果 |
| **必须联网抓取** | 须访问华尔街见闻日历 API；失败带 `full_network` 重试，不可跳过 |
| **失败即重试** | 仍失败则**中断报表生成**——禁止静默回退本地库 |
| **组成** | 当日 `SEED_EVENT_CATALOG` + **当次联网抓取**；再按规则过滤弱催化 |

### 事件覆盖（列表）

**应覆盖（未来 2 周窗口内须尽量提前展示，勿临近才补）：**

| 类型 | 例 | 说明 |
|------|----|------|
| 强主题顶会/展会 | 世界人工智能大会、世界机器人大会、创新药大会 | 板块级情绪窗口 |
| 重要政策窗口 | 国务院常务会议、部委规章/产业规划发布 | 有明确产业映射才保留 |
| 业绩验证窗口 | 中报预告截止、密集预增披露段 | 写清验证与证伪双侧 |
| **产业链级 IPO/申购** | 长鑫、宇树等能带动整条链的节点 | **须 seed 提前写入**；进入 2 周窗口即展示 |

**排除：** 普通单股解禁/无链条 IPO、routine 宏观、区域车展、厂商峰会、通用装备展等弱催化。  
**例外：** 长鑫、宇树等产业链级 IPO **不得**按普通供给过滤。

- **WAIC 筹备吹风/发布会**不单列，并入 WAIC seed 的 brief  
- 列表**日期正序**；seed 优先保留  
- 若 JSON 仍留有 `rhythm`，其中提及且影响板块的重要里程碑可后台补入列表（**非必写**）

#### 事件说明（`brief` · 每次更新强制）

- **标题**只写事件名（如「国务院政策例行吹风会」「香港第一个五年规划公布」）
- **`brief` 只写增量事实**：讲什么、谁出席、核心议题（书面语）；**禁止**复读标题
- **会议类**（大会 / 吹风会 / 论坛 / 峰会 / 发布会等）：**必须**写清本场**具体主题或核心议题**；重要会议优先写入 seed 并全网核实
- **禁止**：「产业顶会或强主题展会窗口」「关注竞价与持续性」、炒作/风险预期

**已定稿（每次更新强制）：** 上述结构、模块头、红蓝点门槛、日期/周几、brief 口径与覆盖范围，**禁止**擅自回潮，除非用户明确要求。

### 研判摘要（已删除 · 禁止回潮）

**页面不再展示** `fwd-lead-wrap` / 节奏峰值 / 2周主线 / 当日背景。  
**更新报表时不再要求** 撰写或维护 `direction_analysis` 的 `summary` / `peak` / `rhythm`。  
历史 JSON 字段可保留；勿恢复展示。§3「预估持续天数」仍管短线持续性；§5 只展示日历事件列表。

#### 字号（固定，勿改）

| 端 | 选择器 | 规格 |
|----|--------|------|
| 桌面 | `.fwd-cal-title` | **13px** / `line-height: 1.5`（对齐 `.post-title`） |
| 桌面 | `.fwd-cal-note` | **12px** / `1.55`（对齐 `.post-summary` / `.sector-text`） |
| 桌面 | `.fwd-cal-md` | **14px** |
| 手机 | `.fwd-cal-title` | **15px** |
| 手机 | `.fwd-cal-note` | **14px** / `1.55` |
| 手机 | `.fwd-cal-md` | **16px** |

## 八维评分算法（固定，勿改回旧版）

页面展示为 **八维**（大盘量能 + 新高指标 + 下表六项追高相关）。其中「新高指标」与六项追高相关共用 `hybrid_hist_score`；大盘量能用独立规则（见下「其他维度」）。

窗口：近 300 个交易日（`dropna` 后有效样本；今追「冲高未回落占比」随数据更新逐步累积）。

```
绝对平均偏离值 = Σ|每日值 − 均值| / 交易日数
均值偏离值     = clamp((当日值 − 均值) / 绝对平均偏离值 × 25, −25, +25)
均值偏离度     = 均值偏离值 + 25
分位           = 分位百分比 × 0.5
总评分         = clamp(分位 + 均值偏离度)
```

| 维度（展示名） | 字段 | 方向 |
|------|------|------|
| 主板活跃 | 主追高数 | 越高越好 |
| 创板活跃 | 创活跃 | 越高越好 |
| 主板昨追 | 主赚差 | 越高越好 |
| 创板昨追 | 创赚差 | 越高越好 |
| 主板今追 | 主冲高未回落占比 | 越高越好 |
| 创板今追 | 创冲高未回落占比 | 越高越好 |
| 新高指标 | 新高 | 越高越好 |

今追取表格「冲高未回落占比」，**不用**收盘强度、**不用**追高回落占比。脚注不写「近X日」，格式：`分位N%（+分），…（偏离值±X，偏离度Y）`。偏离值范围 −25~+25；偏离度 = 偏离值+25（0~50 计分项，中性 25）。

## 八维单元格配色（固定 · `#sec-trend` · 每次更新强制）

A股惯例：**好=红、中=橙、不好=绿**。用于 10 日趋势表 `.trend-cell`（原 `#sec-env` 卡片已删除，配色逻辑不变）。趋势强度占比条的**右侧数字**按相对近200日均值红/绿着色（见上「趋势强度模块」），**不**再打偏强/偏弱 pill。

| 档位 | 分数 | 数字/进度条 `score_bar` | pill「偏强/一般/偏弱」 | callout |
|---|---|---|---|---|
| 好 / 偏强 | ≥60 | `#E53935` 红 | 红底红字（仅 callout 等仍可用） | 红边 |
| 中 / 一般 | 40–59 | `#FF9500` 橙 | 橙 | 橙 |
| 不好 / 偏弱 | &lt;40 | `#34C759` 绿 | 绿底绿字 | 绿边 |

## 其他维度

- **大盘量能**（大盘成交金额）：差=连续2日总缩量>15% / 连续3日缩量 / 缩量且低于前三日均；好=连续2日放量 / 突破前三日最大量；其余中性（42–58，随近3日均比偏移）；说明句写入 `.vol20-note`
- **环境结论**：`build_env_callout` 仅供后台模式判定，**页面不展示**
- **10日趋势**：近 10 个交易日竖轴、**八维**横轴；A股配色红=偏强、绿=偏弱；**不要**再渲染 `trend_headline` 窗口总结 callout；量能/新高柱图改属 **大盘环境**
- **主追效应 / 创追效应**（固定权重，勿改回 5:5）：
  `昨追赚钱效应 × 0.4 + 今追赚钱效应 × 0.6`（今追占比更重；`CHASE_YDAY_W`/`CHASE_TODAY_W`）

## 近30日成交金额柱状图 + 量能说明（固定 · 大盘环境 `#sec-mkt`）

位于 `#sec-mkt`：柱图下方依次为 `.vol20-tags`（有则展示）→ **「量能120日趋势」** → `.vol20-note` → **「历史新高120日趋势」**（模块最底部）。**近30日成交金额**与**量能120日趋势**数据源一致：均为开盘啦「实际量能（沪深京）」`kpl_market_volume.json`（**禁止**再读大盘表成交额）。**每次更新报表必须带上**。

| 项 | 固定规则 |
|---|---|
| 数据源 | **开盘啦**实际量能（沪深京）；与「量能120日趋势」同一缓存 `kpl_market_volume.json` |
| 窗口 | **近 30 个交易日** |
| 纵轴上限 | 近 30 日成交额 **最大值** |
| 纵轴下限 | **近 30 日最小值 × 0.85**（放大波动；禁止从 0 起算，禁止改回 −1000亿 / ×0.9） |
| 柱高 | `(当日 − 下限) / (上限 − 下限) × 100%` |
| 着色 | 较**前一交易日**放量=红（`--bar-ok`）、缩量=绿（`--bar-bad`）、持平=灰；**首日**须与窗口**再前一日**对比，禁止首日一律灰 |
| 单位展示 | 万亿（亿元 ÷ 10000）；标题「近30日成交金额」；meta 含区间 + 最新值 |
| 量能标签 | `.vol20-tags`：差/好各最多 1 条去重短标签；无则不渲染；位置在近30日成交金额图下；**判定序列=开盘啦量能**（与柱图同源） |
| 量能说明 | `.vol20-note`：仅量能数字句（+ 可选「资金在场偏弱」）；不复述标签理由；位置在**量能120日趋势图下**；**数字来自开盘啦** |
| 桌面 | 柱顶可标数值；日期轴稀疏刻度（首 / 约1/3 / 约2/3 / 末） |
| 手机 | **固定一屏、禁止左右滑动**（`overflow: visible` + 柱均分宽度）；柱顶数值隐藏（最新值放标题 meta）；日期轴与柱区分层，末日日期贴右完整显示、不被裁切 |

**量能120日趋势（固定 · 近30日成交金额 / 量能标签之后）：**

| 项 | 固定规则 |
|---|---|
| 数据源 | **开盘啦** App：首页 → 市场情绪 → 市场量能 → **实际量能（沪深京）**；接口 `MarketCapacityKLine`（`Type=0`）+ 当日可叠 `MarketCapacity`；**禁止**再用 `大盘数据.numbers` 成交额列 |
| 缓存 | `kpl_market_volume.json`（`fetch_kpl_volume.py` / `ensure_kpl_volume_series`） |
| 窗口 | **近 120 个交易日** |
| 标题 | **量能120日趋势**（勿改名；勿回潮「量能90日趋势」） |
| 单位 | 展示万亿；接口 `lastPoint` 为万元，先 ÷10000 得亿元再 ÷10000 展示 |
| 纵轴 | 窗口内 min×0.85～max |
| meta | **只写日期区间**（不要「最新 X 万亿」） |
| 着色 | **统一中性灰**（无红绿、无末日描边）；只看形态 |
| 位置 | `.vol20-tags`（有则）**之后**；其后接 `.vol20-note` 量能说明，再接「历史新高120日趋势」；独立 `.vol20-wrap.vol120` |
| 视觉 | 柱更密更细；**始终隐藏**柱顶数字（悬停看数值）；日期刻度首 / 约1/3 / 中点 / 约2/3 / 末 |
| 手机 | 同30日：一屏均分、禁止横滑 |

**量能说明** `.vol20-note`（固定 · 量能120日趋势**下方**）：仅量能数字句（当日万亿 / 近3日均 / 较前日）+ 可选「资金在场偏弱」；不复述标签理由；**禁止**放回近30日成交金额与量能120之间。

**新高数量（固定 · 量能说明之后 · 大盘环境最底部）：**

| 项 | 固定规则 |
|---|---|
| 数据源 | **问财按日累积** `new_high_count_results/series.json` + `days/YYYY-MM-DD.json`；历史已**固化在 series**（不再读大盘表）；八维「新高」若再启用亦同源 |
| 问句（固定） | 仅当报表日=今天用「今日最高价创历史新高，…」；**否则必须用报表日**「YYYY年M月D日最高价创历史新高，非st，非退市，非北京证券交易所，上市天数大于10天」（如周日做周五报表 → 用周五日期，禁止「今日」） |
| 新高定义 | 问财「最高价创历史新高」（后复权口径以问财为准）；**非**短窗口日K自行重算 |
| 每次更新 | 助手用 **Cursor 内置浏览器**跑上述问句 → `python3 new_high_count.py save --date YYYY-MM-DD --count N [--names …]` → 再 `generate_report.py`（问财 `fetch=False`）；**禁止**再手工填大盘表「历史新高」；**禁止**唤起本机 Chrome CDP；**禁止**要求用户报家数 |
| 禁止 | 用约260日K线自行重算「可见新高」；用同花顺数据中心「创历史新高」榜冒充问财问句结果；沿用过期 seed 冒充当日问财；自动开用户 Chrome；让用户口述家数 |
| 展示 | **仅「历史新高120日趋势」**（统一中性灰；隐藏柱顶数字）；meta = 日期区间 + **「最新 N 家」**（当日原始家数）；**已删除**「近30日新高数量」 |
| 纵轴 | 同成交额：窗口内 min×0.85～max |
| 位置 | 「量能120日趋势」**之后**、`#sec-mkt` 最底部 |
| 手机 | 同一屏均分、禁止横滑；隐藏柱顶数字 |

**已定稿（每次更新强制）：** 问财问句 + 历史序列保留 + 按日报表日拉存；页面**只画近120日新高**且 meta 必含「最新 N 家」；**禁止**改用数据中心 cxg、禁止回填覆盖历史 series、禁止非当日仍用「今日」、禁止恢复近30日新高图。

实现注意：日期刻度用 `.vol20-axis` + `.vol20-tick span` 绝对定位；勿把长日期塞进窄柱列内导致重叠/裁切。

## 涨停板块 · 持续性标签（固定三档）

自**报表次一交易日**起算（例：7/9 报表 → 从 7/10 起）：

| 标签 | 含义 |
|------|------|
| **不认可持续** | 不看好次日进一步走强，谨防一日游 |
| **预估持续2天** | 看好次日延续，题材预计还能走 **2 个交易日（含次日）** |
| **预估持续3天及以上** | 看好多日延续，可沿主线缩圈跟龙头 |

与「已持续 X 天」不同；展示文案不再使用「1–2天/2–3天/3–5天」旧格式，弱档 pill **固定**显示「不认可持续」（禁止「不认可持续性」）。

### 已持续天数（固定 · `sector_hot_streak` · 每次更新强制）

数据：开盘啦涨停原因家数（历史日用 `HisHomeDingPan/DailyLimitPerformance` 合并 PidType1–4；**当日**优先列表接口，失败则与出图相同的 `HomeDingPan` 实时聚合互核/写入），缓存 `kpl_sector_history.json`；交易日轴用 `trading_calendar.py`（开盘啦量能）。

| 位置 | 规则 |
|---|---|
| 报表当日 | 该板块有上榜即可计 1 天（**允许 count&lt;5**；因当日若无任何 ≥5 板块，展示的是并列最强，家数可能 &lt;5） |
| 再往前每一天 | **须 count≥5** 才继续累加（除去首日外，后续日 ≥5 才算持续） |
| 往前某日 `0&lt;count&lt;5` | 视为本轮**起点首日**，计入后停止再往前 |
| 某日 count=0 / 无板块 | 停止 |

**禁止**再用「每一天都 ≥5（含首日）」的旧算法。

### 涨停板块序号样式（固定 · 每次更新强制）

| 对象 | 选择器 | 样式 |
|---|---|---|
| 报表五段模块号 | `.section-num` | 实心圆角方块、accent 蓝、白字 |
| 涨停板块列表序 | `.sector-rank` | **浅蓝色实心圆**（`#64B5F6`）、白字 |

生成后若改过 CSS，须确认 `docs/` 最新页里 `.sector-rank` 仍为浅蓝色实心圆，勿改回方块/空心。

## 手机端字号（固定，更新报表时保持不变）

断点：`@media (max-width: 640px)`，定义在 `generate_report.py` 模板 CSS。**除非用户明确要求调整，否则每次生成报表不得改动以下字号。**

### 各模块评分数字（用户指定）

| 模块 | 选择器 | 字号 |
|------|--------|------|
| 1 · 10日趋势 | `#sec-trend .trend-cell` | **13px** |
| 表头 · 模式 | `.hero-mode` | **22px** |
| 表头 · 总结 | `.hero-summary` | **14px**（常规字重，勿加粗） |

### 其余手机端正文（同期确认）

- 正文基准 `body`：**15px**
- 页头标题 `.hero h1`：**24px**；主模式 `.hero-mode`：**22px**
- 表头总结 `.hero-summary`：**14px**、`font-weight: 400`、颜色灰黑 `var(--sub)`
- 模块标题 `.section-title`：**18px**；导航 `.page-nav a`：**14px**
- 板块名 `.sector-name`：**17px**；板块正文 `.sector-text`：**14px**
- 公告标题 `.post-title`：**15px**；公告摘要 `.post-summary`：**14px**
- callout：**15px**
