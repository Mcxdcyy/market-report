---
description: 增长2026 复盘报表固定算法、版式与更新流程
globs: generate_report.py,market_news.json,event_catalog.json
alwaysApply: true
---

# 复盘报表 · 固定输出

**项目路径**：`~/Desktop/增长2026/`  
**数据源**：仅 `大盘数据.numbers`（不用 `大盘复盘.numbers`）  
**生成命令**：`python3 generate_report.py` → `复盘概览-{MM-DD}.html` + `index.html`（手机入口）

**手机访问（局域网）**：`python3 serve_mobile.py`

**外网（GitHub Pages）**：https://mcxdcyy.github.io/market-report/

**Pages 设置**：Settings → Pages → Deploy from a branch → **main** / **docs**（不用 GitHub Actions，Token 只需 **repo** 权限）

**推送**：`bash push_github.sh` 或 `git add docs && git push`

## 报表六段（固定）

| # | 模块 | 说明 |
|---|------|------|
| 1 | 10日趋势 | 近10日竖轴、6指标横轴；无综合环境分 |
| 2 | 环境评分 · 趋势与承接 | 八维（大盘量能+趋势强度+六维）；趋势强度同 hybrid_hist_score；A股红强绿弱 |
| 3 | 涨停板块 Top5 | `market_news.json` → 左逻辑 / 右后续事件+预判；持续性三档见下 |
| 4 | 公告与政策 | `post_close_pool` → `curate_post_close` 精选；**含**上市公司盘后披露 + 当日重要国家政策 |
| 5 | 未来2周 · 事件与方向 | **横向时间轴** → **事件详情** → 题材卡片 → **研判摘要**（节奏峰值 / 2周主线 / 当日背景） |
| 6 | 开仓建议 | 状态横幅 + 承接/模式得分 + 总结 + 判断依据；**无**仓位/试单/加仓/止损四格 |

策略语境：非打板；5/10 日线试单 & 新高加仓；不行就空仓。勿恢复 market_web、综合环境分、开仓四格等已删模块。

## 用户触发表格/报表更新时（必做全套）

**不需要每日自动跑**；用户要求更新时，助手必须一并完成：

1. 读取最新 `大盘数据.numbers` 并运行 `python3 generate_report.py`
2. **涨停板块 Top5**：检索当日市场，写入 `market_news.json` 对应日 `top_sectors`（name/count/reason/codes/stocks）
3. **公告与政策**：同文件 `post_close_pool`，由 `curate_post_close` 精选
   - **上市公司公告**：报表当日 **15:00 收盘后** 正式披露（`timing`: `7/8盘后` 或 `盘后`）
   - **国家政策**：报表当日公开发布的重要文件（国务院/部委规章、出口管制、产业规划等）；`type`: `政策`，`timing` 用 `7/X发布`、`7/X印发`、`7/X盘后` 等标注当日
   - **禁止**写入：前一交易日盘后、无 `timing` 的旧闻复述、盘面点评
   - 代码层 `_is_same_day_post_close` 会自动过滤不符合项（政策允许当日发布，不要求收盘后）
5. **未来2周 · 事件与方向**：按 §5 固定四层排版更新 `direction_analysis` + 事件库；`summary` 必写 `2周主线：…` + `当日背景：…`

## 公告与政策（固定标准 · 每次更新必做）

第 4 模块 **永远** 同时收录两类信息，不可只写上市公司预告/公告：

| 类型 | 要求 | `post_close_pool` 写法 |
|------|------|------------------------|
| 上市公司 | 报表当日 **15:00 收盘后** 正式披露 | `type`: 预告/公告，`timing`: `7/X盘后` |
| 国家政策 | 报表当日公开发布、对板块有炒作价值的文件 | `type`: `政策`，`category`: `D`，`timing`: `7/X发布` / `印发` / `盘后` |

**政策示例**：出口管制（如 7/10 氦气）、国务院批复/部委规章、基药目录、产业规划等。

**每次更新报表时**：检索当日收盘后公告 **+** 当日重要政策，一并写入 `post_close_pool`；晚间批次不全可次日补，但不可漏掉已公开的重大政策。

## 未来2周 · 事件与方向（固定标准 · 每次更新必做）

第 5 模块 **永远** 按下列四层顺序排版（由 `render_fwd_section_html` 生成，**禁止**改回旧版横向圆点+节奏步骤条+峰值双卡片等已删结构）：

| 层级 | 页面区块 | 作用 |
|------|----------|------|
| 1 | **横向时间轴** `fwd-track-wrap` | 一眼看 2 周节奏；按日合并节点，同日多事件缩写（如 `验牌·具身`）；重要节点橙色高亮；**已过期日期不显示** |
| 2 | **事件详情** `fwd-cal-wrap` | 纵向展开每个节点的标题 + brief；峰值窗口打 `催化峰值` / `情绪高点` 标签 |
| 3 | **题材方向** `fwd-themes-wrap` | `themes[]` 卡片网格；stance 配色：优先/可做=红、观察=橙、回避=绿 |
| 4 | **研判摘要** `fwd-lead-wrap`（**放最后**） | 三段标签：**节奏峰值** / **2周主线** / **当日背景** |

### 事件库（时间轴 + 事件详情）

- 只保留**板块级**可炒作节点：AI/机器人/创新药等强主题顶会、政策窗口、中报预告等
- **排除**单股解禁、routine 宏观、区域车展、厂商峰会、通用装备展等弱催化（**长鑫 IPO 等影响整条存储链的除外**，须在 rhythm/peak/themes 体现）
- **WAIC 筹备吹风/发布会**不单列，并入 WAIC seed 的 brief
- 时间轴**日期正序**；seed（WAIC、中报截止）优先保留
- `rhythm` 中提及、且影响板块的重要里程碑（如长鑫 7/16 申购）须补入时间轴/事件详情

### 题材方向研判（direction_analysis）

- 写入 `market_news.json` 对应日期的 `direction_analysis`
- **时间范围：未来 2 周**（不是次日研报）；结合事件时间轴、政策、公告、业绩写主观判断
- 字段：`summary`、`rhythm`、`peak`；`themes[]` 含 `name`、`stance`、`drivers`、`logic`
- **stance 排序**：优先 → 可做 → 观察 → 回避（好 → 中 → 不好）
- **A股配色**（仅 §5 题材卡片）：好=红、中=橙、不好=绿
- **禁止**用环境+涨停加权打分替代主观研判

#### 写法（固定）

| 多写 | 少写 / 不写 |
|------|-------------|
| 2 周日历节点（验牌、IPO、WAIC、CPIC 等）及先后节奏 | 次日竞价、周一开盘、「看承接」等 T+1 话术 |
| `rhythm` / `peak` 与 `themes` 对齐 | 当日涨跌停复盘占满 `summary` |
| 事件时间轴里的**强主题**（WAIC、中报截止、长鑫 IPO 等）须在 `themes[]` 有独立卡片 | 只在 `rhythm` 提一句、却不写主题卡片 |

**字段格式（每次更新严格按此写 JSON）：**

```
peak   → 节奏峰值：未来 2 周内事件最密集、情绪可能最高的日期窗口（1–2 句，不写当日涨跌）
summary → 固定两段，用中文冒号分隔：
         「2周主线：…。当日背景：…。」
         · 2周主线：接下来两周优先看什么方向（业绩预增、会议催化等）
         · 当日背景：今天盘面一句注脚，须写明「不改变上述日历/优先级」类表述
rhythm → 箭头串联的 2 周节奏（供内部对齐，页面不单独展示长条）
themes[] → 每主题独立卡片；logic 写 2 周内催化、验牌/兑现窗口、与相邻事件联动
```

- 当日涨停/大跌**不自动等于** 2 周 stance；弱市下会议催化（如 WAIC）仍可「可做/观察」，须在 `logic` 里写清与验牌、高低切的关系
- **禁止**在研判摘要重复事件详情全文；摘要只做总括，细节留在事件详情与题材卡片

#### 手机端 §5 字号（固定，勿改）

断点 `@media (max-width: 640px)`：`fwd-lead-text` **15px**；`fwd-tlabel` **13px**；`fwd-cal-title` / `fwd-cal-note` **14px**

## 六维评分（固定，勿改回旧版）

窗口：近 300 个交易日（`dropna` 后有效样本；今追「冲高未回落占比」随数据更新逐步累积）。

```
绝对平均偏离值 = Σ|每日值 − 均值| / 交易日数
均值偏离值     = clamp((当日值 − 均值) / 绝对平均偏离值 × 25, −25, +25)
均值偏离度     = 均值偏离值 + 25
分位           = 分位百分比 × 0.5
总评分         = clamp(分位 + 均值偏离度)
```

| 维度 | 字段 | 方向 |
|------|------|------|
| 主板追高 | 主追高数 | 越高越好 |
| 创业板追高 | 创活跃 | 越高越好 |
| 主板昨追 | 主赚差 | 越高越好 |
| 创板昨追 | 创赚差 | 越高越好 |
| 主板今追 | 主冲高未回落占比 | 越高越好 |
| 创板今追 | 创冲高未回落占比 | 越高越好 |
| 趋势强度 | 新高 | 越高越好 |

今追取表格「冲高未回落占比」，**不用**收盘强度、**不用**追高回落占比。趋势强度与上表六维共用 `hybrid_hist_score`（近300日分位+均值偏离度）。脚注不写「近X日」，格式：`分位N%（+分），…（偏离值±X，偏离度Y）`。偏离值范围 −25~+25；偏离度 = 偏离值+25（0~50 计分项，中性 25）。

## 其他维度（非六维）

- **大盘量能**（大盘成交金额）：差=连续2日总缩量>15% / 连续3日缩量 / 缩量且低于前三日均；好=连续2日放量 / 突破前三日最大量；其余中性（42–58，随近3日均比偏移）
- **环境结论**：`build_env_callout` 单条 callout
- **10日趋势**：近 10 个交易日竖轴、6 项指标横轴；A股配色红=偏强、绿=偏弱

## 涨停板块 · 持续性标签（固定三档）

自**报表次一交易日**起算（例：7/9 报表 → 从 7/10 起）：

| 标签 | 含义 |
|------|------|
| **不认可持续** | 不看好次日进一步走强，谨防一日游 |
| **预估持续2天** | 看好次日延续，题材预计还能走 **2 个交易日（含次日）** |
| **预估持续3天及以上** | 看好多日延续，可沿主线缩圈跟龙头 |

与「已持续 X 天」（回溯 Top5 连榜天数）不同；展示文案不再使用「1–2天/2–3天/3–5天」旧格式，弱档 pill **固定**显示「不认可持续」（禁止「不认可持续性」）。

## 手机端字号（固定，更新报表时保持不变）

断点：`@media (max-width: 640px)`，定义在 `generate_report.py` 模板 CSS。**除非用户明确要求调整，否则每次生成报表不得改动以下字号。**

### 各模块评分数字（用户指定）

| 模块 | 选择器 | 字号 |
|------|--------|------|
| 1 · 10日趋势 | `#sec-trend .trend-cell` | **13px** |
| 2 · 环境评分 | `#sec-env .score-val` | **22px** |
| 6 · 开仓建议 | `#sec-trade .gauge-score` | **22px** |

### 其余手机端正文（同期确认）

- 正文基准 `body`：**15px**
- 页头标题 `.hero h1`：**24px**；主模式 `.hero-mode`：**22px**
- 模块标题 `.section-title`：**18px**；导航 `.page-nav a`：**14px**
- 板块名 `.sector-name`：**17px**；板块正文 `.sector-text`：**14px**
- 公告标题 `.post-title`：**15px**；公告摘要 `.post-summary`：**14px**
- 开仓主标签 `.trade-label`：**20px**；callout：**15px**
